# Tell Helm to use an existing PVC
persistence:
  enabled: true
  existingClaim: immich-pvc

# Optional: expose Immich via Tailscale later
server:
  ingress:
    enabled: false  # if not using ingress

# Optional: set Immich-specific values
immich:
  server:
    logLevel: debug